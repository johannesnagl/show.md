name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    name: Swift Tests
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: cd MarkdownRenderer && swift test
